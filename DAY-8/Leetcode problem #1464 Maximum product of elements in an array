// Leetcode #1464 Maximum product of two elements in array//
class Solution {
    public int maxProduct(int[] nums) {
        Arrays.sort(nums);
        int num = (nums[nums.length-1]-1 )*(nums[nums.length-2]-1);
        return num;
    }
}
        
